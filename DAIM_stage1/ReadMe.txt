DAIM Stage1 Application

This is an implementation of DAIM communication channel, which sends and receives OpenFlow messages without any modification.

DAIM adopts the client-server model, where DAIM is the client socket for the OpenFlow controller and also server socket for the switch.

